import { ArrowDownRight, ArrowUpRight, Minus } from 'lucide-react';
export function MetricCard({label,value,change,trend}:{label:string;value:string;change:string;trend:'up'|'down'|'flat'}){ const Icon=trend==='up'?ArrowUpRight:trend==='down'?ArrowDownRight:Minus; return <article className="metric"><div className="metric-top"><p>{label}</p><Icon size={18} aria-hidden="true" /></div><strong>{value}</strong><span className={`trend ${trend}`} aria-label={trend==='up'?`increased by ${change}`:trend==='down'?`decreased by ${change}`:`unchanged: ${change}`}><Icon size={14}/>{change}</span></article>; }
